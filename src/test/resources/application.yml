spring:
  liquibase:
    enabled: false

  datasource:
    url: jdbc:h2:mem:testdb
    username:
    password:

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none

  sql:
    init:
      mode: never